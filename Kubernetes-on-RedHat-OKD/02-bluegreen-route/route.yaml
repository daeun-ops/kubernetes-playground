apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: web
  namespace: team-a
spec:
  to:
    kind: Service
    name: web-blue
    weight: 90
  alternateBackends:
    - kind: Service
      name: web-green
      weight: 10
  port:
    targetPort: 80
  tls:
    termination: edge
